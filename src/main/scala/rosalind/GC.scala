package rosalind

import subscript.language
import subscript.Predef._

import rosalind.util._

object GC extends SubScriptArgsApp with FileIO {script..
  live(args: Array[String]) = fasta(args.head) ~~(smth)~~> println: smth
}